[tox]
envlist = py{310,311,312,313,314}, py{310,311,312,313,314}-gpu, pre-commit

[testenv]
install_command =
    python -I -m pip install {opts} {packages} --extra-index-url https://download.pytorch.org/whl/cpu
extras = dev, preprocess
commands =
    coverage erase
    coverage run -m pytest {posargs}
    coverage combine --quiet
    coverage report

[testenv:py{310,311,312,313,314}-gpu]
install_command =
    python -I -m pip install {opts} {packages}

[testenv:pre-commit]
skip_install = true
deps = pre-commit
commands = pre-commit run --all-files --show-diff-on-failure

[testenv:docs]
install_command =
    python -I -m pip install {opts} {packages} --extra-index-url https://download.pytorch.org/whl/cpu
extras = docs
commands = mkdocs build --strict
